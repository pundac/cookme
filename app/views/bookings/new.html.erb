<div class = "text-center my-3">
    <h1>Complet your cook order</h1>
    <div class = "container border-white rounded bg-white py-3 my-3">
    <p class="text-justify text-center ">Do you want to enjoy <strong><%=@offer.title %></strong></p>
    <p class="text-justify text-center ">from our chef : <strong><%=@offer.user.name %></strong>?</p>
    <p class="text-justify text-center ">Descritption: <%=@offer.description %></p>
    <p class="text-justify text-center "><strong><%=@offer.price %>€</strong></p>
        <div class="form-group">
        <%=simple_form_for [@offer, @booking] do |f| %>
            <%= f.datetime_field :date %>
            <%=f.input :accepted,  as: :radio_buttons %>
            <%= f.button :submit, class: "btn btn-flat" %>
            <%=link_to "Back to offers", offers_path, class: "btn btn-flat" %>
        <% end %>
        </div>
    </div>
</div>
